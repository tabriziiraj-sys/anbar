# 📦 راهنمای استقرار از طریق آپلود فایل ZIP در Liara.ir

## 🎯 روش آپلود ZIP (بدون نیاز به CLI)

این روش برای زمانی مناسب است که نمی‌خواهید از Liara CLI استفاده کنید یا به هر دلیلی دسترسی به ترمینال ندارید.

---

## 📋 مراحل کامل

### مرحله ۱: آماده‌سازی فایل ZIP

#### فایل‌هایی که باید در ZIP باشند:

```
anbarino.zip
├── package.json          ✅ الزامی
├── server.cjs            ✅ الزامی
├── database.cjs          ✅ الزامی
├── liara.json            ✅ الزامی
├── Procfile              ✅ الزامی
├── dist/                 ✅ الزامی
│   ├── index.html
│   └── assets/
├── db/                   ✅ الزامی
│   └── .gitkeep
└── node_modules/         ❌ نباید باشد
```

#### ساخت ZIP صحیح:

**روش ۱: از طریق ترمینال (لینوکس/مک)**
```bash
# در پوشه پروژه
zip -r anbarino.zip \
  package.json \
  server.cjs \
  database.cjs \
  liara.json \
  Procfile \
  dist/ \
  db/
```

**روش ۲: از طریق ترمینال (ویندوز - PowerShell)**
```powershell
# در پوشه پروژه
Compress-Archive -Path package.json, server.cjs, database.cjs, liara.json, Procfile, dist, db -DestinationPath anbarino.zip
```

**روش ۳: از طریق WinRAR یا 7-Zip (ویندوز)**
1. فایل‌های زیر را انتخاب کنید:
   - `package.json`
   - `server.cjs`
   - `database.cjs`
   - `liara.json`
   - `Procfile`
   - پوشه `dist`
   - پوشه `db`
2. راست کلیک → Add to archive
3. فرمت: ZIP
4. OK

**⚠️ مهم:** 
- `node_modules` را **نباید** در ZIP قرار دهید (حجم زیاد)
- Liara خودکار `npm install` را اجرا می‌کند
- ساختار ZIP باید طوری باشد که فایل‌ها در ریشه باشند (نه در یک پوشه اضافی)

---

### مرحله ۲: آپلود در پنل Liara

#### ۲.۱ ورود به پنل

1. وارد [console.liara.ir](https://console.liara.ir) شوید
2. اگر اپلیکیشن ندارید، روی **ایجاد برنامه** کلیک کنید

#### ۲.۲ ساخت اپلیکیشن جدید

1. روی **ایجاد برنامه** کلیک کنید
2. اطلاعات را وارد کنید:
   - **نام برنامه:** `anbarino`
   - **پلتفرم:** `Node.js`
   - **پلن:** `Free` (یا پلن مورد نظر)
   - **منطقه:** `Iran (Tehran)`
3. روی **ایجاد برنامه** کلیک کنید

#### ۲.۳ آپلود فایل ZIP

1. در صفحه اپلیکیشن، روی **استقرار** کلیک کنید
2. روش استقرار را انتخاب کنید: **آپلود فایل ZIP**
3. روی **انتخاب فایل** کلیک کنید
4. فایل `anbarino.zip` را انتخاب کنید
5. روی **آپلود و استقرار** کلیک کنید

#### ۲.۴ مشاهده پیشرفت

- Liara شروع به آپلود می‌کند
- سپس `npm install` اجرا می‌شود
- سپس `npm run build` اجرا می‌شود (از طریق `postinstall`)
- در نهایت سرور اجرا می‌شود

می‌توانید لاگ‌ها را در بخش **لاگ‌ها** مشاهده کنید.

---

### مرحله ۳: ساخت دیسک برای دیتابیس ⚠️ **بسیار مهم**

**بدون دیسک، داده‌ها با هر استقرار از بین می‌روند!**

#### از پنل Liara:

1. در صفحه اپلیکیشن، از منوی سمت چپ روی **دیسک‌ها** کلیک کنید
2. روی **ایجاد دیسک** کلیک کنید
3. اطلاعات را وارد کنید:
   - **نام دیسک:** `database`
   - **اندازه:** `1 GB` (حداقل)
   - **مسیر mount:** `db`
4. روی **ایجاد دیسک** کلیک کنید

#### بررسی `liara.json`:

مطمئن شوید فایل `liara.json` این محتوا را دارد:

```json
{
  "app": "anbarino",
  "port": 3008,
  "disks": [
    {
      "name": "database",
      "mountTo": "db"
    }
  ]
}
```

---

### مرحله ۴: بررسی استقرار

#### ۴.۱ مشاهده لاگ‌ها

1. از منوی سمت چپ، روی **لاگ‌ها** کلیک کنید
2. باید پیام‌های زیر را ببینید:

```
Initializing database...
Database initialized successfully
Seed data inserted

========================================================
       Anbarino - Warehouse Management System
========================================================

  [OK] Server started successfully
  [OK] Port: 3008
  [OK] Database: SQLite

  Access URL:
     http://localhost:3008

  Test Users:
     admin / 1234
     sara / 1234

========================================================
```

#### ۴.۲ دسترسی به اپلیکیشن

آدرس اپلیکیشن شما:

```
https://anbarino.liara.run
```

یا اگر نام دیگری انتخاب کردید:

```
https://[app-name].liara.run
```

---

## 🔧 آیا نیاز به ساخت متغیر محیطی هست؟

### ❌ خیر، نیازی نیست!

**چرا؟**

1. **`PORT`**: Liara به‌صورت خودکار این متغیر را تنظیم می‌کند
2. **`DB_PATH`**: در `database.cjs` مقدار پیش‌فرض تنظیم شده:
   ```javascript
   const DB_PATH = process.env.DB_PATH || path.join(__dirname, 'db', 'anbarino.db');
   ```
3. **`NODE_ENV`**: Liara به‌صورت خودکار `production` را تنظیم می‌کند

### ✅ فقط در صورت نیاز خاص

اگر می‌خواهید مسیر دیتابیس را تغییر دهید:

1. از منوی سمت چپ، روی **متغیرهای محیطی** کلیک کنید
2. روی **افزودن متغیر** کلیک کنید
3. اطلاعات:
   - **نام:** `DB_PATH`
   - **مقدار:** `/app/db/anbarino.db`
4. روی **ذخیره** کلیک کنید

اما **معمولاً نیازی نیست** چون مقدار پیش‌فرض کار می‌کند.

---

## 🔄 به‌روزرسانی اپلیکیشن

### روش ۱: آپلود ZIP جدید

1. در پنل Liara، روی **استقرار** کلیک کنید
2. فایل ZIP جدید را آپلود کنید
3. Liara خودکار استقرار قبلی را جایگزین می‌کند

### روش ۲: استقرار مجدد

اگر فقط تنظیمات را تغییر داده‌اید:

1. روی **استقرار** کلیک کنید
2. روی **استقرار مجدد** کلیک کنید

---

## 📊 مدیریت اپلیکیشن از پنل

### مشاهده آمار

- **داشبورد**: نمایش وضعیت کلی
- **لاگ‌ها**: مشاهده لاگ‌های real-time
- **دیسک‌ها**: مدیریت دیسک‌ها
- **متغیرهای محیطی**: مدیریت env vars
- **دامنه‌ها**: مدیریت دامنه‌های اختصاصی

### مقیاس‌بندی

- **Replicas**: تعداد نمونه‌های برنامه
- **Resources**: منابع CPU و RAM

### تنظیمات پیشرفته

- **Environment Variables**: متغیرهای محیطی
- **Domains**: دامنه‌های اختصاصی
- **Disks**: دیسک‌های متصل
- **Backups**: پشتیبان‌گیری خودکار

---

## 🐛 رفع مشکلات

### مشکل ۱: Build failed

**علت:** خطا در `npm install` یا `npm run build`

**راه‌حل:**
1. لاگ‌ها را بررسی کنید
2. مطمئن شوید `package.json` صحیح است
3. محلی تست کنید: `npm install && npm run build`

### مشکل ۲: Server not starting

**علت:** خطا در `server.cjs` یا `database.cjs`

**راه‌حل:**
1. لاگ‌ها را بررسی کنید
2. مطمئن شوید `start` script در `package.json` وجود دارد
3. محلی تست کنید: `npm start`

### مشکل ۳: دیتابیس ساخته نمی‌شود

**علت:** دیسک ساخته نشده است

**راه‌حل:**
1. به بخش **دیسک‌ها** بروید
2. دیسک `database` را بسازید
3. مسیر mount: `db`
4. استقرار مجدد کنید

### مشکل ۴: داده‌ها از بین می‌روند

**علت:** دیسک ساخته نشده یا مسیر mount اشتباه است

**راه‌حل:**
1. مطمئن شوید دیسک `database` ساخته شده
2. مسیر mount باید `db` باشد
3. در `database.cjs` مسیر باید `db/anbarino.db` باشد

### مشکل ۵: فایل ZIP آپلود نمی‌شود

**علت:** حجم زیاد یا ساختار اشتباه

**راه‌حل:**
1. `node_modules` را حذف کنید
2. مطمئن شوید فایل‌ها در ریشه ZIP هستند
3. حجم ZIP باید کمتر از ۱۰۰MB باشد

---

## 📝 چک‌لیست نهایی

### قبل از آپلود ZIP:

- [ ] `package.json` ویرایش شده (script start و postinstall)
- [ ] `server.cjs` پورت از env می‌خواند ✅
- [ ] `database.cjs` مسیر از env می‌خواند ✅
- [ ] `liara.json` دیسک تعریف شده ✅
- [ ] محلی تست شده: `npm run build && npm start`
- [ ] فایل ZIP ساخته شده (بدون node_modules)

### بعد از آپلود:

- [ ] اپلیکیشن ساخته شده
- [ ] **دیسک `database` ساخته شده** ⚠️
- [ ] لاگ‌ها بررسی شده (سرور اجرا شده)
- [ ] آدرس اپلیکیشن کار می‌کند
- [ ] ورود به سیستم موفقیت‌آمیز

---

## 🎯 خلاصه مراحل

```
1. ساخت ZIP (بدون node_modules)
   ↓
2. آپلود در پنل Liara
   ↓
3. صبر برای نصب و build
   ↓
4. ساخت دیسک (مهم!)
   ↓
5. بررسی لاگ‌ها
   ↓
6. دسترسی به اپلیکیشن
```

---

## 💡 نکات مهم

### ۱. حجم ZIP

- `node_modules` را قرار ندهید
- حجم ZIP باید کمتر از ۱۰۰MB باشد
- Liara خودکار `npm install` را اجرا می‌کند

### ۲. ساختار ZIP

- فایل‌ها باید در ریشه ZIP باشند
- نباید در یک پوشه اضافی باشند
- ساختار صحیح:
  ```
  anbarino.zip
  ├── package.json
  ├── server.cjs
  └── dist/
  ```

### ۳. دیسک الزامی

- بدون دیسک، داده‌ها موقتی هستند
- با هر استقرار، داده‌ها از بین می‌روند
- حتماً دیسک بسازید!

### ۴. متغیرهای محیطی

- معمولاً نیازی به ساخت دستی نیست
- `PORT` توسط Liara تنظیم می‌شود
- `DB_PATH` مقدار پیش‌فرض دارد

---

## 📞 پشتیبانی

- مستندات Liara: [docs.liara.ir](https://docs.liara.ir)
- تیکت پشتیبانی: از پنل Liara
- مستندات SQLite در Liara: [docs.liara.ir/paas/nodejs/how-tos/connect-to-db/sqlite](https://docs.liara.ir/paas/nodejs/how-tos/connect-to-db/sqlite/)

---

## 🎉 موفق باشید!

پروژه شما آماده آپلود ZIP و استقرار روی Liara.ir است.

**آدرس نهایی:** `https://anbarino.liara.run`

**کاربرهای آزمایشی:**
- `admin / 1234`
- `sara / 1234`
